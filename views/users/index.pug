html
  head
  body
    h1 Users
    a(href="/users/create") Add user
    
    form(action="/users/search", method="GET") 
      input(type="text", name="q", value=curent )
      button(type="submit") Search

    each user in users
      li= user.name

    if users.length === 0
      p Từ khóa bạn tìm hiện không có

